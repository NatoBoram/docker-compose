services:
  anubis:
    container_name: anubis
    depends_on:
      - caddy
    env_file:
      - path: .env.anubis
      - path: .env.anubis.local
    environment:
      COOKIE_HTTP_ONLY: true
      COOKIE_SAME_SITE: Strict
      TARGET: " "
    image: ghcr.io/techarohq/anubis
    networks:
      - caddy-anubis
    restart: always
    volumes:
      - ./volumes/cfg:/data/cfg

      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
